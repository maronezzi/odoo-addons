<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="cep.list">
      <field name="name">cep list</field>
      <field name="model">cep.cep</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="cep.action_window">
      <field name="name">cep window</field>
      <field name="res_model">cep.cep</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="cep.action_server">
      <field name="name">cep server</field>
      <field name="model_id" ref="model_cep_cep"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="cep" id="cep.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="cep.menu_1" parent="cep.menu_root"/>
    <menuitem name="Menu 2" id="cep.menu_2" parent="cep.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="cep.menu_1_list" parent="cep.menu_1"
              action="cep.action_window"/>
    <menuitem name="Server to list" id="cep" parent="cep.menu_2"
              action="cep.action_server"/>
    -->
  </data>
</odoo>